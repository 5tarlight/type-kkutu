extends layout

block css
  link(rel="stylesheet", href="/routes/views/css/search.css")

block title
  title 검색

block content
  - for(let i in results) {
  - let link = '/w/' + results[i]['title']
    div
      a(href=link) #{results[i]['title']}
      div
        i #{results[i]['description']}
    sh
  - }
  